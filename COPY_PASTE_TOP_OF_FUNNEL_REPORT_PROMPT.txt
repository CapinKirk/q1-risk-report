# TOP OF FUNNEL MARKETING PACING REPORT - COMPREHENSIVE PROMPT

## Context
You are helping generate a Marketing Top of Funnel pacing report for a paid ads vendor meeting. This report tracks MQL → SQL → SQO pacing for two products (POR and R360) across three regions (AMER, EMEA, APAC) for the INBOUND channel.

**Report Period:** Q1 2026 QTD (January 1 to current date)
**Products:** POR (Point of Rental) and R360 (Record360)
**Regions:** AMER, EMEA, APAC
**Channel:** INBOUND only

---

## STEP 1: Run BigQuery Queries

Run these queries in BigQuery to get current actuals and targets.

### Query 1: POR Inbound Funnel Actuals
**IMPORTANT:** MQL uses CaptureDate (when lead was captured), not MQL_DT

```sql
SELECT
  CASE Division
    WHEN 'US' THEN 'AMER'
    WHEN 'UK' THEN 'EMEA'
    WHEN 'AU' THEN 'APAC'
  END AS region,
  COUNT(DISTINCT CASE WHEN MQL_DT IS NOT NULL AND CAST(CaptureDate AS DATE) >= '2026-01-01' AND CAST(CaptureDate AS DATE) <= CURRENT_DATE() THEN COALESCE(LeadEmail, ContactEmail) END) AS mql,
  COUNT(DISTINCT CASE WHEN SQL_DT IS NOT NULL AND CAST(SQL_DT AS DATE) >= '2026-01-01' AND CAST(SQL_DT AS DATE) <= CURRENT_DATE() THEN COALESCE(LeadEmail, ContactEmail) END) AS sql,
  COUNT(DISTINCT CASE WHEN SAL_DT IS NOT NULL AND CAST(SAL_DT AS DATE) >= '2026-01-01' AND CAST(SAL_DT AS DATE) <= CURRENT_DATE() THEN COALESCE(LeadEmail, ContactEmail) END) AS sal,
  COUNT(DISTINCT CASE WHEN SQO_DT IS NOT NULL AND CAST(SQO_DT AS DATE) >= '2026-01-01' AND CAST(SQO_DT AS DATE) <= CURRENT_DATE() THEN COALESCE(LeadEmail, ContactEmail) END) AS sqo
FROM `data-analytics-306119.MarketingFunnel.InboundFunnel`
WHERE Division IN ('US', 'UK', 'AU')
  AND (SpiralyzeTest IS NULL OR SpiralyzeTest = false)
  AND (MQL_Reverted IS NULL OR MQL_Reverted = false)
GROUP BY region
ORDER BY region;
```

### Query 2: R360 Inbound Funnel Actuals
**IMPORTANT:** MQL uses CaptureDate (when lead was captured), not MQL_DT

```sql
SELECT
  Region AS region,
  COUNT(DISTINCT CASE WHEN MQL_DT IS NOT NULL AND CAST(CaptureDate AS DATE) >= '2026-01-01' AND CAST(CaptureDate AS DATE) <= CURRENT_DATE() THEN Email END) AS mql,
  COUNT(DISTINCT CASE WHEN SQL_DT IS NOT NULL AND CAST(SQL_DT AS DATE) >= '2026-01-01' AND CAST(SQL_DT AS DATE) <= CURRENT_DATE() THEN Email END) AS sql,
  COUNT(DISTINCT CASE WHEN Demo_DT IS NOT NULL AND CAST(Demo_DT AS DATE) >= '2026-01-01' AND CAST(Demo_DT AS DATE) <= CURRENT_DATE() THEN Email END) AS demo,
  COUNT(DISTINCT CASE WHEN SQO_DT IS NOT NULL AND CAST(SQO_DT AS DATE) >= '2026-01-01' AND CAST(SQO_DT AS DATE) <= CURRENT_DATE() THEN Email END) AS sqo
FROM `data-analytics-306119.MarketingFunnel.R360InboundFunnel`
WHERE Region IS NOT NULL
  AND MQL_Reverted = false
GROUP BY Region
ORDER BY Region;
```

### Query 3: QTD Targets from Strategic Operating Plan
```sql
SELECT
  RecordType AS product,
  Region AS region,
  ROUND(SUM(Target_MQL), 0) AS target_mql,
  ROUND(SUM(Target_SQL), 0) AS target_sql,
  ROUND(SUM(Target_SAL), 0) AS target_sal,
  ROUND(SUM(Target_SQO), 0) AS target_sqo
FROM `data-analytics-306119.Staging.StrategicOperatingPlan`
WHERE Percentile = 'P50'
  AND Source = 'INBOUND'
  AND OpportunityType != 'RENEWAL'
  AND TargetDate >= '2026-01-01'
  AND TargetDate <= CURRENT_DATE()
  AND RecordType IN ('POR', 'R360')
GROUP BY RecordType, Region
ORDER BY RecordType, Region;
```

---

## STEP 2: Provide Query Results

After running the queries, provide the results in this format:

```
POR ACTUALS:
- AMER: MQL=XX, SQL=XX, SAL=XX, SQO=XX
- EMEA: MQL=XX, SQL=XX, SAL=XX, SQO=XX
- APAC: MQL=XX, SQL=XX, SAL=XX, SQO=XX

R360 ACTUALS:
- AMER: MQL=XX, SQL=XX, Demo=XX, SQO=XX
- EMEA: MQL=XX, SQL=XX, Demo=XX, SQO=XX
- APAC: MQL=XX, SQL=XX, Demo=XX, SQO=XX

TARGETS (QTD):
- POR AMER: MQL=XX, SQL=XX, SQO=XX
- POR EMEA: MQL=XX, SQL=XX, SQO=XX
- POR APAC: MQL=XX, SQL=XX, SQO=XX
- R360 AMER: MQL=XX, SQL=XX, SQO=XX
- R360 EMEA: MQL=XX, SQL=XX, SQO=XX
- R360 APAC: MQL=XX, SQL=XX, SQO=XX

Today's Date: YYYY-MM-DD
Days into Q1: XX of 90
```

---

## STEP 3: Generate Slack Report

Using the data provided, generate a Slack-ready report with this exact format:

```
🚨 MARKETING FUNNEL PACING — Q1 2026 QTD
📅 Jan 1-[TODAY] (XX% through Q1)
📊 Source: BigQuery MarketingFunnel tables

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 POR INBOUND FUNNEL

AMER:
MQL: [actual] of [target] ([pct]%) → [gap] gap [emoji]
SQL: [actual] of [target] ([pct]%) → [gap] gap [emoji]
SQO: [actual] of [target] ([pct]%) → [gap] gap [emoji]

EMEA:
MQL: [actual] of [target] ([pct]%) → [gap] gap [emoji]
SQL: [actual] of [target] ([pct]%) → [gap] gap [emoji]
SQO: [actual] of [target] ([pct]%) → [gap] gap [emoji]

APAC:
MQL: [actual] of [target] ([pct]%) → [gap] gap [emoji]
SQL: [actual] of [target] ([pct]%) → [gap] gap [emoji]
SQO: [actual] of [target] ([pct]%) → [gap] gap [emoji]

───────────────────────────────────
POR TOTAL:
MQL: [sum] of [sum] ([pct]%) → [gap] gap [emoji]
SQL: [sum] of [sum] ([pct]%) → [gap] gap [emoji]
SQO: [sum] of [sum] ([pct]%) → [gap] gap [emoji]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 R360 INBOUND FUNNEL

[Same format as POR]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 BIGGEST GAPS (Ranked by Count)

#1 [emoji] [Product] [Region] [Stage]: [gap] ([pct]% pacing)
#2 [emoji] [Product] [Region] [Stage]: [gap] ([pct]% pacing)
#3 [emoji] [Product] [Region] [Stage]: [gap] ([pct]% pacing)
[Continue for top 7 gaps]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📈 CONVERSION ANALYSIS

POR Funnel Health:
• AMER: MQL→SQL [pct]% | SQL→SQO [pct]%
• EMEA: MQL→SQL [pct]% | SQL→SQO [pct]%
• APAC: MQL→SQL [pct]% | SQL→SQO [pct]%

R360 Funnel Health:
• AMER: MQL→SQL [pct]% | SQL→SQO [pct]%
• EMEA: MQL→SQL [pct]% | SQL→SQO [pct]%
• APAC: MQL→SQL [pct]% | SQL→SQO [pct]%

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 KEY INSIGHTS

1. [Biggest issue with data point]
2. [Second issue with data point]
3. [Bright spot if any]
4. [Regional pattern]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💰 REVENUE AT RISK (Est.)

Using 10% MQL→Won, $5K avg deal:
• [Product] [Region]: [gap] MQLs → ~$[amount]K at risk
[List top 3-4]
───────────────────────────────────
TOTAL: ~$[sum]K Q1 revenue at risk

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❓ QUESTIONS FOR VENDOR

1. Why is [worst segment] at [pct]% pacing?
2. What's driving [best segment] success?
3. Is [struggling region] getting ad spend?
4. CPL trend by region last 30 days?
5. Where's the recovery plan?

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 SUMMARY

COMBINED FUNNEL GAPS:
• MQL: [gap] ([pct]% pacing)
• SQL: [gap] ([pct]% pacing)
• SQO: [gap] ([pct]% pacing)

🚨 CRITICAL: [Top issue]
🚨 CRITICAL: [Second issue]
✅ BRIGHT SPOT: [What's working]
```

---

## Emoji Legend for Status

Use these emojis based on pacing percentage:
- 🟢 GREEN: ≥90% pacing (on track)
- 🟡 YELLOW: 70-89% pacing (at risk)
- 🔴 RED: <70% pacing (miss)
- ⚪ N/A: Zero target or divide by zero

For gaps:
- Positive gap (ahead): show as "+X ahead"
- Negative gap (behind): show as "-X gap"

---

## Data Sources Reference

| Metric | POR Source | R360 Source |
|--------|------------|-------------|
| MQL | InboundFunnel.CaptureDate + MQL_DT NOT NULL | R360InboundFunnel.CaptureDate + MQL_DT NOT NULL |
| SQL | InboundFunnel.SQL_DT | R360InboundFunnel.SQL_DT |
| SAL | InboundFunnel.SAL_DT | R360InboundFunnel.Demo_DT |
| SQO | InboundFunnel.SQO_DT | R360InboundFunnel.SQO_DT |
| Targets | StrategicOperatingPlan | StrategicOperatingPlan |

**Key Filters:**
- POR: SpiralyzeTest = false, MQL_Reverted = false
- R360: MQL_Reverted = false
- Targets: Percentile = 'P50', Source = 'INBOUND', OpportunityType != 'RENEWAL'

**IMPORTANT: MQL Date Logic:**
- Use `CaptureDate` to filter the date range (when lead was captured)
- Require `MQL_DT IS NOT NULL` to ensure lead qualified as MQL
- This matches dashboard numbers more accurately than using MQL_DT for date filtering

---

## Example Output

If query results show:
- POR AMER: MQL=61, SQL=34, SQO=6 (targets: 111, 55, 31)
- R360 AMER: MQL=22, SQL=9, SQO=11 (targets: 82, 29, 23)

Then calculate:
- POR AMER MQL: 61/111 = 55% pacing, gap = -50
- R360 AMER MQL: 22/82 = 27% pacing, gap = -60

Revenue at risk for R360 AMER MQL gap:
- 60 MQLs × 10% win rate × $5K = $30K at risk

---

## Instructions

1. Run the three BigQuery queries
2. Paste the results into this chat
3. I will generate the complete Slack-ready report
4. Copy the report directly into Slack

The report will auto-calculate:
- Pacing percentages
- Gaps (actual - target)
- Conversion rates
- Revenue at risk estimates
- Status emojis
- Ranked gap list
