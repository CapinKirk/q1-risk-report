# API Key for protecting the /api/refresh endpoint
# Generate a secure random string for production
REFRESH_API_KEY=your-secure-api-key-here

# Google Cloud credentials (for local development)
# Note: For production deployment, use Vercel environment variables
# and configure BigQuery service account credentials properly
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# BigQuery Project (optional - defaults to data-analytics-306119)
# BIGQUERY_PROJECT=your-project-id
